# Legacy standalone

Open `index.html` in a browser with a local server, or use any static file host.

This build uses CDN React + Babel and loads `../ios-frame.jsx` from the repo root.

The Vite app (`npm run dev` from repo root) is the migration target; screen components will move into `src/` incrementally.
